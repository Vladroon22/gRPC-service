syntax = "proto3";

option go_package = "github.com/Vladroon22/gRPC-service/pkg/proto;proto";

service Server {
    rpc BroadCast(ClientText) returns (ServerText) {}
}

message ClientText {
    string text = 1;
}

message ServerText {
    string response = 1;
}
