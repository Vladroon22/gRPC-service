syntax = "proto3";

option go_package = "pkg/proto/";

service Translater {
    rpc Translate(Request) returns (Response) {}
}

message Request {
    string query = 1;
}

message Response {
    string response = 1;
}
